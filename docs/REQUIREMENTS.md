# 機能要件定義書
## Instagram バズコンテンツ制作システム

**バージョン:** 1.0.0
**作成日:** 2025-11-23
**ステータス:** Draft

---

## 1. システム概要

### 1.1 目的
Instagramで注目を集めるリール動画を分析し、AIを活用して「バズる可能性の高い」コンテンツを効率的に作成するシステム。

### 1.2 対象ユーザー
- SNSマーケター
- インフルエンサー
- コンテンツクリエイター
- 企業のSNS担当者

### 1.3 システム構成図

```
┌─────────────────────────────────────────────────────────────┐
│                    Instagram-buzz System                     │
├─────────────────────────────────────────────────────────────┤
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────────────┐  │
│  │ F1: 検索    │→│ F2: DL      │→│ F3: 台本変換        │  │
│  │ バズリール  │  │ 動画取得    │  │ 動画→テキスト      │  │
│  └─────────────┘  └─────────────┘  └─────────────────────┘  │
│         ↓                                    ↓              │
│  ┌─────────────────────────────────────────────────────┐    │
│  │              F4: バズ理由分析エンジン                │    │
│  │              (Claude API)                           │    │
│  └─────────────────────────────────────────────────────┘    │
│         ↓              ↓              ↓           ↓        │
│  ┌───────────┐  ┌───────────┐  ┌───────────┐  ┌─────────┐  │
│  │F5: Threads│  │F6: リール │  │F7: Caption│  │F8: Reply│  │
│  │投稿生成   │  │台本生成   │  │生成       │  │生成     │  │
│  └───────────┘  └───────────┘  └───────────┘  └─────────┘  │
└─────────────────────────────────────────────────────────────┘
```

---

## 2. 機能要件

### F1: バズリール検索機能

| 項目 | 内容 |
|------|------|
| **機能ID** | F1 |
| **機能名** | バズリール検索機能 |
| **優先度** | P1-High |
| **関連Issue** | #2 |

#### 2.1.1 機能概要
ジャンル別キーワード検索でInstagram上のバズリールを発見する。

#### 2.1.2 入力
| パラメータ | 型 | 必須 | 説明 |
|-----------|-----|------|------|
| keyword | string | ✓ | 検索キーワード（例：心理学、節約術、簡単レシピ） |
| period | number | ✓ | 期間フィルター（日数、デフォルト180日） |
| min_views | number | ✓ | 最小再生数（デフォルト30,000） |
| limit | number | - | 取得件数（デフォルト10） |

#### 2.1.3 出力
```typescript
interface BuzzReel {
  id: string;
  url: string;
  title: string;
  views: number;
  likes: number;
  comments: number;
  posted_at: Date;
  author: {
    username: string;
    followers: number;
  };
}
```

#### 2.1.4 処理フロー
1. キーワードでInstagram検索APIを呼び出し
2. 期間フィルター適用（半年以内）
3. 再生数フィルター適用（3万回以上）
4. エンゲージメント率でソート
5. 結果をリスト形式で返却

---

### F2: 動画ダウンロード機能

| 項目 | 内容 |
|------|------|
| **機能ID** | F2 |
| **機能名** | 動画ダウンロード機能 |
| **優先度** | P1-High |
| **関連Issue** | #3 |

#### 2.2.1 機能概要
リール動画URLからローカルに動画ファイルをダウンロードする。

#### 2.2.2 入力
| パラメータ | 型 | 必須 | 説明 |
|-----------|-----|------|------|
| url | string | ✓ | InstagramリールURL |
| output_path | string | - | 保存先パス |

#### 2.2.3 出力
```typescript
interface DownloadResult {
  success: boolean;
  file_path: string;
  file_size: number;
  duration: number;
  format: string;
}
```

#### 2.2.4 外部連携
- snapinsta.to API連携
- 代替: yt-dlp ライブラリ

---

### F3: 動画→台本変換機能

| 項目 | 内容 |
|------|------|
| **機能ID** | F3 |
| **機能名** | 動画→台本変換機能 |
| **優先度** | P1-High |
| **関連Issue** | #4 |

#### 2.3.1 機能概要
動画ファイルをAIで解析し、完全な台本（スクリプト）を生成する。

#### 2.3.2 入力
| パラメータ | 型 | 必須 | 説明 |
|-----------|-----|------|------|
| video_path | string | ✓ | 動画ファイルパス |
| language | string | - | 言語（デフォルト: ja） |

#### 2.3.3 出力
```typescript
interface Script {
  full_text: string;
  segments: {
    start_time: number;
    end_time: number;
    text: string;
    speaker?: string;
  }[];
  summary: string;
  keywords: string[];
}
```

#### 2.3.4 処理フロー
1. 動画から音声を抽出
2. Whisper API で音声認識
3. NotebookLM または Claude で構造化
4. タイムスタンプ付き台本生成

#### 2.3.5 使用プロンプト
```
添付のセミナーを完璧に再現するために、完璧な台本を生成してください。
もしもボリュームが多い場合は、分割して出力してください。
情報を一部も漏らしてはいけません。
```

---

### F4: バズ理由分析機能

| 項目 | 内容 |
|------|------|
| **機能ID** | F4 |
| **機能名** | バズ理由分析機能 |
| **優先度** | P1-High |
| **関連Issue** | #5 |

#### 2.4.1 機能概要
台本を分析し、なぜそのコンテンツがバズったかを徹底分析する。

#### 2.4.2 入力
| パラメータ | 型 | 必須 | 説明 |
|-----------|-----|------|------|
| script | string | ✓ | 台本テキスト |
| metrics | object | - | 再生数、いいね数等 |

#### 2.4.3 出力
```typescript
interface BuzzAnalysis {
  hook: {
    type: string;
    effectiveness: number;
    description: string;
  };
  structure: {
    opening: string;
    body: string;
    closing: string;
  };
  emotional_triggers: string[];
  viral_factors: {
    factor: string;
    score: number;
    explanation: string;
  }[];
  target_audience: string;
  recommendations: string[];
}
```

#### 2.4.4 使用プロンプト
```
添付のコンテンツがバズった理由を詳細に分析してください。

##原本##
（台本テキスト）
```

---

### F5: Threads投稿生成機能

| 項目 | 内容 |
|------|------|
| **機能ID** | F5 |
| **機能名** | Threads投稿生成機能 |
| **優先度** | P1-High |
| **関連Issue** | #6 |

#### 2.5.1 機能概要
バズ分析結果を活かした新しいThreads用リプライ型投稿を生成する。

#### 2.5.2 入力
| パラメータ | 型 | 必須 | 説明 |
|-----------|-----|------|------|
| script | string | ✓ | 元の台本 |
| analysis | BuzzAnalysis | ✓ | バズ分析結果 |
| title | string | ✓ | 関心を引くタイトル |

#### 2.5.3 出力
```typescript
interface ThreadsPost {
  post1: {
    text: string;
    char_count: number;
  };
  post2: {
    text: string;
    char_count: number;
  };
  hashtags: string[];
}
```

#### 2.5.4 制約条件
- 太字使用不可
- 絵文字使用不可
- 2段階構成必須
- 日本語で一般的にわかりやすい表現

#### 2.5.5 使用プロンプト
```
添付の２段階の文章構成で、「（タイトル）」に関して、
バズ分析を全て取り入れ、視点や角度を変えて、
一般的な日本人にわかりやすい表現でリプライ型の
バズるThreads新たな投稿を２段階にまとめて作ってください。
太字、絵文字不要です。

##原本##
（台本テキスト）
```

---

### F6: リール台本生成機能

| 項目 | 内容 |
|------|------|
| **機能ID** | F6 |
| **機能名** | リール台本生成機能 |
| **優先度** | P1-High |
| **関連Issue** | #7 |

#### 2.6.1 機能概要
元の台本をベースに、新しい視点・切り口のワンページリール台本を生成する。

#### 2.6.2 入力
| パラメータ | 型 | 必須 | 説明 |
|-----------|-----|------|------|
| script | string | ✓ | 元の台本 |
| template | string | - | テンプレート |
| angle | string | - | 切り口の指定 |

#### 2.6.3 出力
```typescript
interface ReelScript {
  title: string;
  hook: string;
  main_content: {
    point: string;
    detail: string;
  }[];
  cta: string;
  duration_estimate: number;
  visual_notes: string[];
}
```

#### 2.6.4 使用プロンプト
```
添付のテンプレートをもとに、下記元ネタの内容から、
視点や角度を変えてワンページリール台本を生成してください。

##原本##
（台本テキスト）
```

---

### F7: キャプション生成機能

| 項目 | 内容 |
|------|------|
| **機能ID** | F7 |
| **機能名** | キャプション生成機能 |
| **優先度** | P1-High |
| **関連Issue** | #8 |

#### 2.7.1 機能概要
リール動画の説明文（キャプション）を自動生成する。

#### 2.7.2 入力
| パラメータ | 型 | 必須 | 説明 |
|-----------|-----|------|------|
| script | string | ✓ | 台本テキスト |
| profile | string | - | SNSプロフィール |
| template | string | - | キャプションテンプレート |

#### 2.7.3 出力
```typescript
interface Caption {
  main_text: string;
  hashtags: string[];
  cta: string;
  char_count: number;
  seo_score: number;
}
```

#### 2.7.4 使用プロンプト
```
添付のSNSプロフィールとテンプレートを参考に、
下記元ネタの内容から、視点や角度を変えて
ワンページリールキャプションを生成してください。

##原本##
（台本テキスト）
```

---

### F8: コメント返信生成機能

| 項目 | 内容 |
|------|------|
| **機能ID** | F8 |
| **機能名** | コメント返信生成機能 |
| **優先度** | P2-Medium |
| **関連Issue** | #9 |

#### 2.8.1 機能概要
コメントに対する印象の良い返信、または投稿への感情を揺さぶるコメントを生成する。

#### 2.8.2 入力
| パラメータ | 型 | 必須 | 説明 |
|-----------|-----|------|------|
| post_content | string | ✓ | 投稿内容 |
| comment | string | - | 受け取ったコメント（返信モード時） |
| mode | string | ✓ | "reply" または "comment" |

#### 2.8.3 出力
```typescript
interface CommentSuggestion {
  suggestions: {
    text: string;
    tone: string;
    emotional_impact: number;
  }[];
}
```

#### 2.8.4 使用プロンプト（返信モード）
```
添付のポストに対して、下記のコメントがありました。
印象の良い返信アイディアをおねがいします。

##もらったコメント##
（コメント内容）
```

#### 2.8.5 使用プロンプト（コメントモード）
```
添付のコンテンツに対して、印象の良くかつ
感情が揺さぶられるコメントアイディアをおねがいします。
```

---

## 3. 非機能要件

### 3.1 パフォーマンス要件

| 項目 | 要件 |
|------|------|
| API応答時間 | 5秒以内（台本生成除く） |
| 台本生成時間 | 30秒以内 |
| 同時リクエスト | 10件/分 |
| ファイルサイズ上限 | 100MB |

### 3.2 セキュリティ要件

| 項目 | 要件 |
|------|------|
| API認証 | JWT Bearer Token |
| データ暗号化 | TLS 1.3 |
| APIキー管理 | 環境変数（.env） |
| ログ保持期間 | 30日 |

### 3.3 可用性要件

| 項目 | 要件 |
|------|------|
| 稼働率 | 99.5% |
| バックアップ | 日次 |
| リカバリ時間 | 4時間以内 |

---

## 4. 技術スタック

### 4.1 フロントエンド
- Next.js 14
- TypeScript
- Tailwind CSS

### 4.2 バックエンド
- Node.js / Express
- Python (AI処理)

### 4.3 AI/ML
- Claude API (Anthropic)
- Whisper API (OpenAI)
- NotebookLM (Google)

### 4.4 インフラ
- Vercel (Frontend)
- Railway / Render (Backend)
- Supabase (Database)

---

## 5. データモデル

```typescript
// ユーザー
interface User {
  id: string;
  email: string;
  profile: {
    username: string;
    bio: string;
    sns_accounts: string[];
  };
  created_at: Date;
}

// プロジェクト
interface Project {
  id: string;
  user_id: string;
  name: string;
  genre: string;
  created_at: Date;
}

// 分析結果
interface Analysis {
  id: string;
  project_id: string;
  source_url: string;
  script: string;
  buzz_analysis: BuzzAnalysis;
  generated_contents: {
    threads_post?: ThreadsPost;
    reel_script?: ReelScript;
    caption?: Caption;
  };
  created_at: Date;
}
```

---

## 6. API エンドポイント

| Method | Endpoint | 説明 |
|--------|----------|------|
| POST | /api/search | バズリール検索 |
| POST | /api/download | 動画ダウンロード |
| POST | /api/transcribe | 台本変換 |
| POST | /api/analyze | バズ理由分析 |
| POST | /api/generate/threads | Threads投稿生成 |
| POST | /api/generate/script | リール台本生成 |
| POST | /api/generate/caption | キャプション生成 |
| POST | /api/generate/comment | コメント生成 |

---

## 7. マイルストーン

| フェーズ | 機能 | 状態 |
|---------|------|------|
| Phase 1 | F1, F2, F3 (データ収集) | 📥 pending |
| Phase 2 | F4 (分析エンジン) | 📥 pending |
| Phase 3 | F5, F6, F7 (コンテンツ生成) | 📥 pending |
| Phase 4 | F8 (コミュニケーション支援) | 📥 pending |

---

## 8. 関連ドキュメント

- [マスターIssue #1](https://github.com/ryoma3736/Instagram-buzz/issues/1)
- [F1: バズリール検索 #2](https://github.com/ryoma3736/Instagram-buzz/issues/2)
- [F2: 動画ダウンロード #3](https://github.com/ryoma3736/Instagram-buzz/issues/3)
- [F3: 台本変換 #4](https://github.com/ryoma3736/Instagram-buzz/issues/4)
- [F4: バズ分析 #5](https://github.com/ryoma3736/Instagram-buzz/issues/5)
- [F5: Threads生成 #6](https://github.com/ryoma3736/Instagram-buzz/issues/6)
- [F6: 台本生成 #7](https://github.com/ryoma3736/Instagram-buzz/issues/7)
- [F7: キャプション #8](https://github.com/ryoma3736/Instagram-buzz/issues/8)
- [F8: コメント #9](https://github.com/ryoma3736/Instagram-buzz/issues/9)

---

*Generated by Miyabi Framework*
